""" A session is a way of marking a browser  and storing data on the server which 
can be stored and retrieved across multiple request-response-cycles
Sessions exist irrespective of whether or not the user is logged in
When the user passes the login check, the server adds data to the session i
dentifying the user
When the user logs out, that information in the session is removed

 """